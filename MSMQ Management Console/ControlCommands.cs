
namespace MSMQManagementConsole
{
    internal static class ControlCommands
    {
        public const string ExitCommand = "exit"; //Exits console
        public const string HelpCommand = "?"; //Displays help message
        public const string LoadCommand = "load"; //Loads commands from the source file, in the single command mode executing them one by one in the multicommand mode staking them in command cache
    }
}
